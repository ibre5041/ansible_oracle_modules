---
- hosts: oracle
  gather_facts: false
  environment:
    ORACLE_SID: "{{ oracle_sid | default('') }}"
    ORACLE_HOME: "{{ oracle_home | default('') }}"
  tasks:
    - include_tasks: tasks/main.yml
