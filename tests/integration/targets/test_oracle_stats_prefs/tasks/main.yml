---

- name: DBMS_STATS global preferences tests
  block:
    - include_tasks: "set_reset.yml"

  environment:
    ORACLE_SID: "{{ oracle_sid | default(omit) }}"
    ORACLE_HOME: "{{ oracle_home | default(omit) }}"

...
