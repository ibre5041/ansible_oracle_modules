---

- name: "define connection parameters"
  set_fact:
    connection_parameters: &con_param
      hostname: "{{ oracle_hostname }}"
      port: "{{ oracle_port }}"
      service_name: "{{ (running_on_server|bool) | ternary(omit,oracle_pdb_service_name|default(oracle_service_name)) }}"
      session_container: "{{ (not running_on_server|bool) | ternary(omit,oracle_pdb_service_name|default(oracle_service_name)) }}"      
      username: "{{ oracle_pdb_username | default(oracle_username) }}"
      password: "{{ oracle_pdb_password | default(oracle_password) }}"
      dsn: "{{ oracle_dsn | default() }}"
      mode: "{{ mode }}"
      oracle_home: "{{ oracle_home | default() }}"      

- debug: var=connection_parameters      

- name: "reset user"
  oracle_user:
    <<: *con_param
    schema_name: "foo"
    state: "absent"

- name: "delete a system user (not authorized)"
  oracle_user:
    <<: *con_param
    schema_name: "system"
    state: "absent"
  register: _
  failed_when: not _.failed or 'Trying to drop an internal user' not in _.msg

- name: "delete an inexistant user"
  oracle_user:
    <<: *con_param
    schema_name: "inexistant_user"
    state: "absent"
  register: _
  failed_when: _.failed or _.changed

...
