---
- name: "define connection parameters"
  set_fact:
    connection_parameters: &con_param
      hostname: "{{ oracle_hostname }}"
      port: "{{ oracle_port }}"
      service_name: "{{ oracle_service_name }}"
      username: "{{ oracle_username }}"
      password: "{{ oracle_password }}"
      mode: "sysdba"

- name: 'Connection parameters - see or use integration_config.yml.template'
  debug:
    var: connection_parameters

- name: create tablespace in check mode
  oracle_tablespace:
    <<: *con_param
    tablespace: "ts"
    size: "1M"
    datafiles: "/tmp/ts.dbf"
    state: "present"
    bigfile: no
  register: _
  failed_when: not _.changed
  check_mode: yes

- name: drop tablespaces don't do anything
  oracle_tablespace:
    <<: *con_param
    tablespace: "ts"
    state: "absent"
  register: _
  failed_when: _.changed
...
